RUN apt-get update \
    && apt-get -y --no-install-recommends install \
      clang-3.8 \
    && rm -rf /var/lib/apt/lists/* \
    && ln -s /usr/bin/clang-3.8 /usr/bin/clang \
    && ln -s /usr/bin/clang++-3.8 /usr/bin/clang++
